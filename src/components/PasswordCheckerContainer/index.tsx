import React from 'react';
import styled from 'styled-components';
import { Button } from '../Button';

export const PasswordCheckerContainer = () => {
  return (
    <div>
      <p>Password checker</p>
      <Button>
        Submit
      </Button>
    </div>
  );
}

